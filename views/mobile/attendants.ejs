<div class="jumbotron" style="height:1000px">
  <div class="card" style="margin-top:-20px;">
    <div class="card-header">
      Attendants
    </div>
    <ul class="list-group">
      <% for (var attendant of attendants) { %>
        <li class="list-group-item"><%= attendant.name %></li>
      <% } %>
    </ul>
  </div>
</div>